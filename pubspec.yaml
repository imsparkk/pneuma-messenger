name: pneuma_messenger
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.5.0
  cloud_firestore: 6.1.3
  firebase_auth: ^6.2.0
  firebase_storage: ^13.1.0
  email_validator: ^3.0.0
  intl: ^0.19.0
  image_picker: ^1.0.0
  cached_network_image: ^3.3.0
  video_player: ^2.8.0
  provider: ^6.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
